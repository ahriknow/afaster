# AFaster 文档

本目录包含 AFaster 框架的完整文档，使用 [mdBook](https://rust-lang.github.io/mdBook/) 构建。

```bash
mdbook build docs   # 构建 HTML 文档
mdbook serve docs   # 本地预览（默认 http://localhost:3000）
```

文档源文件位于 `docs/src/`，构建产物输出到 `docs/book/`。
